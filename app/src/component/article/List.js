import React, {Component, PropTypes} from 'react';

import '../../assets/pages/article/list.scss';

export default class List extends Component {
  static displayName = 'ArticleList';

  static propTypes = {
    // todo 添加需要的参数
    children: PropTypes.any
  }

  render() {
    // todo 添加具体的内容
    return null;
  }
}
